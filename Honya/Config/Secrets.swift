import Foundation

// FICHIER GÉNÉRÉ PAR LA CI AU MOMENT DU BUILD.
//
// La version committée dans le dépôt est volontairement VIDE : la vraie valeur
// vit dans une variable d'environnement chiffrée de Codemagic
// (GOOGLE_BOOKS_API_KEY) et n'est écrite ici que sur la machine de build,
// juste avant la compilation. Elle n'apparaît donc ni dans le code source,
// ni dans l'historique git, ni dans une interface de l'app.
enum Secrets {
    static let cleGoogleBooks = ""
}
